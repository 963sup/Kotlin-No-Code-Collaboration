# Robolectric defaults to the module targetSdk. The application currently targets
# API 36, while the pinned Robolectric runtime in this repository supports API 35.
# Keep JVM tests deterministic until the test runtime is upgraded.
sdk=35
